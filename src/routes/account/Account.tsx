import React, { useEffect } from "react";
import { useLocation } from "wouter";
import AuthWrapper from "../../components/layout/AuthWrapper";

const Account: React.FC = () => {
  return <AuthWrapper>ACCOUNT</AuthWrapper>;
};

export default Account;
